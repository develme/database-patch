<?php

return [


    /*
    |--------------------------------------------------------------------------
    | Patch Repository Table
    |--------------------------------------------------------------------------
    |
    | This table keeps track of all the patches that have already run for
    | your application. Using this information, we determine which of
    | the patches on disk haven't actually been run in the database.
    |
    */

    'table' => 'patches',
];